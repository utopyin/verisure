{
  "created_at": "2026-06-08T16:10:25.865Z",
  "id": "1dac80e4",
  "status": "closed",
  "tags": [
    "server",
    "security",
    "context"
  ],
  "title": "Implement credential crypto and request context tags"
}

Reconciled as complete.

Implemented in the current codebase:

- `CredentialCrypto` with Web Crypto-compatible AES-GCM credential encryption/decryption keyed from `RuntimeConfig`.
- Encryption/decryption support for Verisure email, password, and optional PIN.
- Request context tags: `CurrentUser`, `CurrentCredential`, and `CurrentInstallation`.
- Request-scope helpers in `RequestScope.ts` for credential ownership and verified installation scoping.
- Services and RPC middleware use context tags instead of repeated user/credential/installation arguments.

Validation has been run during later slices: `bun check` and `bun run test` pass.
