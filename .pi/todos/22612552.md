{
  "id": "22612552",
  "title": "Add end-to-end workerd and Alchemy smoke tests",
  "tags": [
    "testing",
    "alchemy",
    "workerd",
    "e2e"
  ],
  "status": "open",
  "created_at": "2026-06-08T16:11:38.407Z"
}

Read first:
- `INITIAL_PRD.md` Testing Decisions
- `docs/architecture.md` sections: Testing strategy, Cloudflare and Alchemy resources
- Existing Alchemy example integration tests in `repos/alchemy/examples/cloudflare-worker*/test`

Goal:
Prove the full stack works at the highest seam practical: Alchemy/workerd with web + API + bindings.

Scope:
- Add smoke tests or documented test commands that run the API Worker under Alchemy/workerd-compatible dev/test mode.
- Verify `/api/health` returns success.
- Verify Config-backed secrets bind and required missing config fails clearly.
- Verify D1 persists a minimal user/credential/token/export row.
- Verify SendEmail can be stubbed locally for magic-link flow.
- Verify Durable Object session store can persist a per-credential session snapshot.
- Verify protected RPC rejects unauthenticated calls and accepts a stub authenticated context if possible.
- Verify Shortcut REST rejects missing token and accepts a valid test token against stubbed Verisure services.

Dependencies:
- Depends on `TODO-532e947b` for API Worker mounts.
- Depends on `TODO-bb7fb45f` only if smoke includes web navigation; API-only smoke can start earlier.
- Production custom-domain smoke depends on `TODO-8b0386bd`.

Testing/validation:
- This task itself creates the smoke tests.
- Keep tests deterministic; do not hit real Verisure in smoke tests.

Notes:
- Prefer stubbing Verisure upstream at the transport boundary.
- Keep all tests colocated or in app-specific test locations, not a root test folder.
