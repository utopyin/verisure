{
"id": "619fc684",
"title": "Implement TanStack Start web app RPC client and query layer",
"tags": [
"web",
"tanstack",
"rpc"
],
"status": "open",
"created_at": "2026-06-08T16:11:23.911Z"
}

Read first:

- `INITIAL_PRD.md` dashboard and TanStack Query user stories
- `docs/architecture.md` sections: HTTP and RPC surface, Key call graphs / Dashboard query through RPC, Monorepo package layout
- Effect RPC client examples in `repos/effect/packages/platform-node/test/fixtures/rpc-e2e.ts`

Goal:
Build the frontend RPC client and TanStack Query wrappers in `apps/web`.

Scope:

- Set up TanStack Start app structure if not already scaffolded.
- Build one Effect RPC client for DashboardRpcs targeting relative `/api/rpc`.
- Ensure browser cookies are included for Better Auth sessions.
- Implement TanStack Query wrappers and mutation helpers for credentials, status, installations, arm state, alarm mutations, devices, shortcut export, API token listing/revocation.
- Centralize query keys and invalidation rules.
- Keep the web app from importing `packages/server` internals.

Dependencies:

- Depends on `TODO-1b7a729e` for apps/web scaffolding.
- Depends on `TODO-23f38d31` for RPC contract.
- Can be developed against a stub/mock RPC server before `TODO-532e947b`, but full integration depends on `TODO-532e947b`.

Testing/validation:

- Colocated tests verify query keys are stable.
- Tests verify mutations invalidate expected keys.
- Tests verify client targets `/api/rpc` and does not require shortcut bearer tokens for dashboard calls.

Notes:

- This task is frontend infrastructure, not full dashboard UI polish.
