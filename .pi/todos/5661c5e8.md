{
"id": "5661c5e8",
"title": "Wire production custom domain deploy and final acceptance checks",
"tags": [
"deploy",
"alchemy",
"cloudflare"
],
"status": "open",
"created_at": "2026-06-08T16:11:47.279Z"
}

Read first:

- `INITIAL_PRD.md` Solution and Further Notes
- `docs/architecture.md` sections: Cloudflare and Alchemy resources, Key design decisions
- Todo `TODO-8b0386bd` for the local Alchemy custom-domain branch

Goal:
Complete deployment wiring and validate the intended production surface.

Scope:

- Ensure this repo uses the local Alchemy package with custom Worker route support after `TODO-8b0386bd` is done.
- Configure routes so backend API is served at `https://verisure.utopy.sh/api/*` and RPC at `https://verisure.utopy.sh/api/rpc/*`.
- Ensure the web app can call the API with relative `/api/rpc` and `/api/auth/*` paths in dev and production.
- Verify Alchemy deploy output includes useful URLs/resource IDs.
- Run final acceptance checks for magic-link auth, credential add/MFA status flow with stub or controlled Verisure behavior, dashboard RPC calls, shortcut token export, and Shortcut REST token auth.

Dependencies:

- Depends on `TODO-8b0386bd` for local Alchemy route support.
- Depends on `TODO-532e947b` for API Worker mounts.
- Depends on `TODO-619fc684` for web RPC client path behavior.
- Depends on `TODO-22612552` for smoke test coverage.

Testing/validation:

- `alchemy dev` works locally.
- `alchemy deploy` or dry-run equivalent validates resources.
- Production route checks hit `/api/health` and `/api/rpc` correctly.
- No secret values appear in logs or deploy output.

Notes:

- This is a final integration/deploy task, not a feature implementation task.
