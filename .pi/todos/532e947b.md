{
"assigned_to_session": "019ed500-78a9-7def-b7ce-57410543d5df",
"created_at": "2026-06-08T16:11:16.613Z",
"id": "532e947b",
"status": "open",
"tags": [
"api",
"http",
"rest",
"cloudflare"
],
"title": "Implement API Worker HTTP mounts and Shortcut REST routes"
}

Read first:

- `INITIAL_PRD.md` Solution and Shortcut REST user stories
- `docs/architecture.md` sections: Cloudflare and Alchemy resources, Layer graph, HTTP and RPC surface, Key call graphs / Shortcut REST request
- Alchemy Cloudflare Worker examples in `repos/alchemy/examples/cloudflare-worker*`

Goal:
Build `apps/api` as the single backend Worker entrypoint mounting health, Better Auth, RPC, and Shortcut REST.

Scope:

- Implement `worker.ts`, `Http/App.ts`, `BetterAuthMount`, `RpcMount`, `RestApi`, route scope helpers, and centralized REST error mapping.
- Mount `GET /api/health`.
- Mount Better Auth under `/api/auth/*` through BetterAuthService.fetch.
- Mount Effect RPC under `/api/rpc/*` using DashboardRpcs and HandlersLive.
- Implement Shortcut REST endpoints: status, toggle full, explicit mode.
- Authenticate Shortcut REST with `Authorization: Bearer <token>` and provide CurrentUser/CurrentCredential/CurrentInstallation.
- Bind D1, SendEmail, Durable Object namespace, KV cache if used, rate limit, and Config-backed secrets through Alchemy.
- Ensure API route shape is compatible with `verisure.utopy.sh/api/*` custom domain routing.

Dependencies:

- Depends on `TODO-1b7a729e` for app/Alchemy skeleton.
- Depends on `TODO-a3b66ad4` for BetterAuthService and EmailService.
- Depends on `TODO-d722ee44` for RPC handlers.
- Depends on `TODO-3293a61a` for API token auth.
- Depends on `TODO-13e1922c` for alarm/status services.
- Production custom-domain validation depends on `TODO-8b0386bd`.

Testing/validation:

- HTTP tests verify `/api/health` works without auth.
- REST tests verify missing/invalid/revoked/expired tokens fail safely.
- REST tests verify route validation and error mapping.
- Workerd/Alchemy smoke test starts the Worker with bindings.

Notes:

- This is the integration point. If lower-level services are incomplete, use stubs only in tests, not production code.

Progress update — Dashboard RPC HTTP mount slice:

- Added `apps/api/src/Http/RpcMount.ts` with an Effect RPC HTTP handler built from `DashboardRpcContract` and `HandlersLive`.
- Uses `RpcServer.toHttpEffect(...)` with `RpcSerialization.layerNdjson`.
- Mounted `/api/rpc` and `/api/rpc/*` in `apps/api/src/worker.ts` instead of returning the previous `dashboard-rpc` placeholder.
- Expanded `ApiWorkerLive` to provide the service graph needed by Dashboard RPC handlers:
  - Better Auth
  - repositories / database
  - credential crypto
  - Verisure requests/session store
  - credential/installation/alarm/device services
- Sequenced dependent layers with `Layer.provideMerge(...)` rather than merging dependent layers in parallel.
- Removed stale unused `apps/api/src/Layers.ts` which exported an obsolete partial `ApiWorkerLive`.

Still pending in this TODO:

- Shortcut REST routes under `/api/v1/*`.
- API token authentication for Shortcut REST.
- Workerd/Alchemy smoke tests for mounted routes.

Validation:

- `bun fix` passes.
- `bun check` passes.
- `bun run test` passes.

Layer ownership refinement:

- Added `Server.ApplicationServicesLive` in `packages/server/src/Services/index.ts`.
- `ApplicationServicesLive` composes the dashboard/application services and owns their internal shared production dependencies:
  - `VerisureRequests.Live`
  - `CredentialCrypto.Live`
- Simplified `apps/api/src/worker.ts` so the Worker no longer wires `Server.VerisureRequests.Live` or `Server.CredentialCrypto.Live` directly.
- `ApiWorkerLive` now composes API-level concerns:
  - `Server.ApplicationServicesLive`
  - `Server.BetterAuthService.Live`
  - persistence/platform/base layers

Validation:

- `bun fix` passes.
- `bun check` passes.
- `bun run test` passes.
