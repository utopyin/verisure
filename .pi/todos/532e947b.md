{
  "assigned_to_session": "019ed500-78a9-7def-b7ce-57410543d5df",
  "created_at": "2026-06-08T16:11:16.613Z",
  "id": "532e947b",
  "status": "open",
  "tags": [
    "api",
    "http",
    "rest",
    "cloudflare"
  ],
  "title": "Implement API Worker HTTP mounts and Shortcut REST routes"
}

Reconciled as still open, but narrowed.

Already implemented:

- API Worker entrypoint in `apps/api/src/worker.ts`.
- Health route.
- Better Auth mount under `/api/auth/*` via `BetterAuthService.fetch`.
- Dashboard RPC mount under `/api/rpc/*` using Effect RPC and `HandlersLive`.
- Worker layer graph wiring for config, DB/repositories, Better Auth, application services, Durable Object session store, browser crypto, and Cloudflare bindings.
- Central safe error mapper in `apps/api/src/Http/ErrorMapper.ts`.
- API token and Shortcut export services are now implemented and wired into Shortcut RPC handlers.

Still pending for this TODO:

- Implement Shortcut REST routes under `/api/v1/*`:
  - `GET /api/v1/alarm/status?giid=:giid`
  - `POST /api/v1/alarm/toggle-full`
  - `POST /api/v1/alarm/mode`
- Authenticate Shortcut REST with `Authorization: Bearer <token>` using `ApiTokenService.authenticate`.
- Provide `CurrentUser`, `CurrentCredential`, and verified `CurrentInstallation` for REST handlers.
- Validate REST query/body payloads with Effect Schema.
- Add REST tests for missing/invalid/revoked/expired tokens, route validation, scope/GIID checks, and safe error mapping.
- Workerd/Alchemy smoke tests remain for the later smoke-test TODO.

Recommended next slice: implement only Shortcut REST and its focused tests.
