{
"created_at": "2026-06-08T16:10:39.235Z",
"id": "184f6a0c",
"status": "closed",
"tags": [
"server",
"durable-object",
"session"
],
"title": "Implement Durable Object Verisure session store"
}

Read first:

- `INITIAL_PRD.md` session-related implementation decisions
- `docs/architecture.md` sections: Session storage, Key call graphs / Verisure session refresh-login
- Alchemy Cloudflare Worker/Durable Object examples in `repos/alchemy/examples/cloudflare-worker*`

Goal:
Build per-credential Verisure session storage using a Durable Object, replacing Python's cookie file.

Scope:

- Implement `VerisureSessionObject` export in `apps/api` or a server package as appropriate.
- Implement `VerisureSessionStore` service that uses one object per `verisureCredentialId` / CurrentCredential.
- Store session snapshots: cookies, trust token, preferred base URL, authenticated/expiry times, and temporary MFA cookies.
- Provide `withCredentialLock` semantics so login/refresh/cookie mutation is serialized for a credential.
- Bind the Durable Object namespace through Alchemy and the API Worker.

Dependencies:

- Depends on `TODO-1b7a729e` for Alchemy DO skeleton.
- Depends on `TODO-1dac80e4` for CurrentCredential context.
- Depends on `TODO-4b2b3ea0` for cookie snapshot types/helpers if needed.

Testing/validation:

- Colocated tests verify get/put/clear and MFA temp state behavior.
- Tests verify concurrent refresh/login calls are serialized or otherwise cannot corrupt the snapshot.
- Workerd-compatible smoke test should prove the DO binding is usable by the API Worker.

Notes:

- KV may cache non-sensitive status/installations but must not store credentials, PINs, or Verisure session cookies.

Progress:

- Added `VerisureSessionStore` service in `packages/server/src/Verisure/VerisureSessionStore.ts` with session snapshot/MFA state types, get/put/clear operations, and `withCredentialLock`.
- Added an in-memory test layer for service-level tests.
- Implemented the Alchemy Durable Object in `apps/api/src/SessionObject.ts` with snapshot/MFA persistence and lock acquire/release RPC methods.
- Added `apps/api/src/SessionStoreLive.ts` to bridge the server service to one Durable Object instance per current credential.
- Wired the DO and session store into `apps/api/src/worker.ts`.
- Added colocated tests for snapshot persistence, MFA temp state, and in-memory credential lock serialization.

Validation:

- `bun check` passes.
- `bun run test` passes.

Self-review fixes applied:

- Made MFA state independent from authenticated snapshots so `putMfaState` cannot create a fake authenticated session snapshot.
- Fixed the in-memory lock layer to install per-credential semaphores atomically under concurrent first access.
- Made DO lock release best-effort/logged in the finalizer instead of turning a successful critical section into a defect if release fails.
- Reduced public surface by keeping DO RPC helper types and the store shape local.
- Re-ran validation after fixes: `bun check` and `bun run test` pass.

Review feedback fix:

- Re-checked Alchemy Durable Object guide and corrected RPC members to be callable methods returning `Effect`s instead of bare `Effect` properties.
- Updated the DO-backed session store layer to call `stub.getSnapshot()`, `stub.clearSnapshot()`, `stub.getMfaState()`, and `stub.clearMfaState()`.
- Re-ran validation: `bun check` and `bun run test` pass.

Simplification after Durable Object storage docs review:

- Removed explicit `state.storage.transaction(...)` wrappers from lock acquire/release.
- The lock compare-and-set now uses top-level DO storage `get` followed immediately by `put`/`delete` with no unrelated I/O between them, matching current SQLite-backed Durable Object storage guidance.
- Checked the Effect translation: with Alchemy's storage wrapper, `put` is lazy until yielded, so "do not await" cannot be translated by simply not yielding the Effect; for this single-write lock update, yielding the `put` is fine and keeps backpressure/error handling. The important part for automatic atomic behavior is no unrelated async I/O between the storage read and write.
- Re-ran validation: `bun check` and `bun run test` pass.

Final review:

- Deferred the real workerd/Alchemy smoke test to a later hardening slice; current slice is covered by server unit tests plus API typechecking.
- Final diff review passed: DO RPC members are callable methods, MFA state is separate from authenticated snapshots, lock acquire/release no longer use explicit storage transactions, and public helper/API surface stays minimal.
