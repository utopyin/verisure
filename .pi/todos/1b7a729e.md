{
  "id": "1b7a729e",
  "title": "Bootstrap Bun monorepo and Alchemy stack skeleton",
  "tags": [
    "setup",
    "monorepo",
    "alchemy"
  ],
  "status": "open",
  "created_at": "2026-06-08T16:09:51.084Z"
}

Read first:
- `INITIAL_PRD.md`
- `docs/architecture.md` sections: Cloudflare and Alchemy resources, Monorepo package layout, Key design decisions
- Existing repo files: `package.json`, `tsconfig.json`, `AGENTS.md`

Goal:
Create the workspace skeleton for the planned Verisure app without implementing feature logic yet.

Scope:
- Convert the repo to a Bun workspace with `apps/api`, `apps/web`, `packages/domain`, `packages/rpc-contract`, `packages/db`, and `packages/server`.
- Add package manifests and TS configs suitable for Effect/Alchemy/workerd.
- Add root scripts for `alchemy dev`, `alchemy deploy`, `alchemy destroy`, `alchemy logs`, and tests.
- Create a root `alchemy.run.ts` stack skeleton with Cloudflare providers, Drizzle providers, state, D1, SendEmail, Durable Object namespace, optional KV cache, rate limit binding, Vite web app, and API Worker placeholders.
- Use Effect Config (`Config.redacted`) for `BETTER_AUTH_SECRET`, `CREDENTIAL_ENCRYPTION_KEY`, and `TOKEN_PEPPER`; do not add Cloudflare SecretsStore.

Dependencies:
- None for local scaffolding.
- Production custom route validation depends on `TODO-8b0386bd`.

Testing/validation:
- `bun install` succeeds.
- TypeScript project references or package TS configs are coherent.
- `bun run` scripts resolve.
- No feature behavior required yet.

Notes:
- Keep tests colocated in future work; do not create a root `test/` tree.
- Package manager is Bun; runtime target is workerd.
