{
  "id": "1b7a729e",
  "title": "Bootstrap Bun monorepo and Alchemy stack skeleton",
  "tags": [
    "setup",
    "monorepo",
    "alchemy"
  ],
  "status": "closed",
  "created_at": "2026-06-08T16:09:51.084Z"
}

Read first:
- `INITIAL_PRD.md`
- `docs/architecture.md` sections: Cloudflare and Alchemy resources, Monorepo package layout, Key design decisions
- Existing repo files: `package.json`, `tsconfig.json`, `AGENTS.md`

Goal:
Create the workspace skeleton for the planned Verisure app without implementing feature logic yet.

Scope completed:
- Converted root package to a Bun workspace with `apps/api`, `apps/web`, `packages/domain`, `packages/rpc-contract`, `packages/db`, and `packages/server`.
- Added package manifests and TS configs for all apps/packages.
- Added root scripts: `dev`, `deploy`, `destroy`, `logs`, `tail`, `test`, and `typecheck`.
- Added root `alchemy.run.ts` skeleton with Cloudflare providers, Drizzle providers, state, D1, SendEmail, Durable Object namespace, KV cache, rate-limit binding, Vite web app, API Worker, custom route pattern, and Effect Config-backed secrets.
- Added API Worker placeholder routes for `/api/health`, `/api/auth/*`, `/api/rpc/*`, and `/api/v1/*`.
- Added `VerisureSessionObject` Durable Object placeholder export.
- Added minimal web scaffold and RPC/query placeholder files.
- Added package placeholder exports for domain, rpc-contract, db, and server.
- Added a colocated domain scaffold test.
- Added `lib` / `**/lib` to `.gitignore` because `tsc -b` emits declaration outputs for composite project references.

Dependencies:
- Production custom route support was unblocked by closed `TODO-8b0386bd`.

Validation:
- `bun install` succeeds.
- `bun run typecheck` succeeds.
- `bun run test` succeeds and only scans `./packages` and `./apps`, avoiding vendored `repos/` tests.
- `bun alchemy --help` resolves the linked local Alchemy CLI. It prints a Bun internal directory-mismatch warning for the linked package, but CLI help still loads.

Notes for next workers:
- Feature behavior is intentionally placeholder-only. Follow-on todos implement actual domain, DB, auth/email, Verisure services, RPC handlers, REST routes, and UI.
- Root `alchemy.run.ts` currently uses a small `as any` cast for merged Cloudflare + Drizzle providers due provider typing friction in the linked local Alchemy package.

Follow-up adjustment requested by user:
- Added Turborepo setup after the initial monorepo scaffold.
- Added root `turbo.json` with `build`, `check:types`, `test`, `dev`, and `deploy` tasks plus Cloudflare/auth config pass-through env keys.
- Added `turbo` dev dependency and root scripts: `build`, `check:types`, `typecheck`, `dev:turbo`, and `test:turbo`.
- Added package-level `build` / `check:types` scripts for apps and packages; domain also has a colocated `test` script.
- Kept root `dev` as `alchemy dev` because Alchemy remains the base command for the full stack dev server.
- Validation after Turbo setup: `bun install`, `bun run check:types`, `bun run test`, `bun run build`, and `bun run test:turbo` all succeeded.

Code review follow-up:
- Replaced package-level `check:types` scripts from `tsc -p tsconfig.json --noEmit` to plain `tsc` in all apps/packages.
- Added shared `tsconfig.base.json` containing the common compiler options, including `noEmit: true`.
- Simplified app/package `tsconfig.json` files to extend the shared base and include local source files.
- Removed TS project-reference arrays from package configs; Turborepo now models package task ordering.
- Updated root `check:types`/`typecheck` to typecheck root `alchemy.run.ts` and then run package typechecks through Turbo.
- Adjusted Turbo build outputs to `[]` because package builds are typecheck-only scaffolds for now.
- Validation after review fix: `bun run check:types`, `bun run build`, and `bun run test` succeed.
