{
  "id": "8b0386bd",
  "title": "Rebase local Alchemy custom-domain routes branch and link it into verisure",
  "tags": [
    "alchemy",
    "cloudflare",
    "side-quest"
  ],
  "status": "closed",
  "created_at": "2026-06-08T15:31:24.168Z"
}

Read first:
- `INITIAL_PRD.md` Further Notes
- `docs/architecture.md` sections: Cloudflare and Alchemy resources, Monorepo package layout, Key design decisions
- Alchemy repo at `/Users/utopy/Documents/Void/Dev/alchemy-effect`

Goal:
Make this repo use the local Alchemy implementation that supports custom Worker routes for the target API domain.

Scope completed:
- Checked out `/Users/utopy/Documents/Void/Dev/alchemy-effect` branch `feat/cloudflare-worker-routes`.
- Rebasing on `main` reported the branch was already up to date.
- Flattened `listWorkerRoutesInZones` in `packages/alchemy/src/Cloudflare/Workers/Worker.ts` to remove the nested ternary/inline nested mapping and make the per-zone route listing easier to read.
- Ran `bunx oxfmt packages/alchemy/src/Cloudflare/Workers/Worker.ts`.
- Ran `cd /Users/utopy/Documents/Void/Dev/alchemy-effect && bun run --filter alchemy build`; build completed successfully, with the existing `cloudflare:workers` unresolved-import warning treated as external.
- Committed on `feat/cloudflare-worker-routes`: `70a6bc95 Flatten worker route listing`.
- Registered local package with `cd /Users/utopy/Documents/Void/Dev/alchemy-effect/packages/alchemy && bun link`.
- Linked it in this repo with `bun link alchemy --save`, resulting in `alchemy: link:alchemy` and `node_modules/alchemy -> ../../alchemy-effect/packages/alchemy`.
- Verified `bun pm ls alchemy` shows `alchemy@link:alchemy` and an import of `alchemy/Cloudflare` resolves.

Unlocks:
- `TODO-1b7a729e` production custom route portions.
- `TODO-532e947b` production route validation.
- `TODO-5661c5e8` final deploy/custom-domain acceptance.

Notes:
- This repo has uncommitted `package.json` and `bun.lock` changes from `bun link alchemy --save`.
- The Alchemy branch is ahead of origin by 1 local commit; it has not been pushed.
