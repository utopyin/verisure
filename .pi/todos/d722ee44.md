{
  "created_at": "2026-06-08T16:11:08.175Z",
  "id": "d722ee44",
  "status": "closed",
  "tags": [
    "api",
    "rpc",
    "effect"
  ],
  "title": "Implement dashboard RPC handlers and middleware"
}

Read first:

- `INITIAL_PRD.md` dashboard user stories and RPC testing decisions
- `docs/architecture.md` sections: HTTP and RPC surface, Layer graph, Key call graphs / Dashboard query through RPC
- `packages/rpc-contract` once implemented
- Effect RPC examples in `repos/effect/packages/platform-node/test/fixtures/rpc-schemas.ts`

Goal:
Implement the server side of DashboardRpcs in `apps/api` using server services and context middleware.

Scope completed:

- Implemented API-owned `AuthMiddleware` using BetterAuthService to provide `Server.CurrentUser`.
- Implemented credential and installation scope middleware to provide `Server.CurrentCredential` and `Server.CurrentInstallation` from RPC payloads.
- Implemented `HandlersLive` exactly matching the API-composed RPC contract.
- Mapped service errors to typed RPC errors from the contract.
- Ensured credential-scoped handlers verify ownership through middleware before calling services.
- Kept dashboard behavior on RPC; Shortcut export / API token behavior remains deferred to `TODO-3293a61a` and returns typed unsupported errors for now.

Implementation notes:

- Reorganized dashboard RPC server code around API-owned middleware:
  - `AuthMiddleware` provides `Server.CurrentUser`.
  - `CredentialScopeMiddleware` verifies `credentialId` ownership and provides `Server.CurrentCredential`.
  - `InstallationScopeMiddleware` verifies `giid` membership and provides `Server.CurrentInstallation`.
- Split handler implementation by RPC group and composed them in `apps/api/src/Rpc/index.ts`.
- Mounted the composed group through `apps/api/src/Http/RpcMount.ts`.
- Added `apps/api/src/Rpc/index.test.ts` using `RpcTest.makeClient(Rpcs)` against `HandlersLive` with stubbed server services.

Tests cover:

- unauthenticated calls failing with typed `Unauthorized`;
- credential ownership failure mapped to typed `CredentialNotFound`;
- installation membership failure mapped to typed `InstallationNotFound`;
- successful handler delegation with request-scoped `CurrentUser`, `CurrentCredential`, and `CurrentInstallation` provided by middleware.

Validation:

- `bun fix` passes.
- `bun check` passes.
- `bun run test` passes.
