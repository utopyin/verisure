{
  "id": "d722ee44",
  "title": "Implement dashboard RPC handlers and middleware",
  "tags": [
    "api",
    "rpc",
    "effect"
  ],
  "status": "open",
  "created_at": "2026-06-08T16:11:08.175Z"
}

Read first:
- `INITIAL_PRD.md` dashboard user stories and RPC testing decisions
- `docs/architecture.md` sections: HTTP and RPC surface, Layer graph, Key call graphs / Dashboard query through RPC
- `packages/rpc-contract` once implemented
- Effect RPC examples in `repos/effect/packages/platform-node/test/fixtures/rpc-schemas.ts`

Goal:
Implement the server side of DashboardRpcs in `apps/api` using server services and context middleware.

Scope:
- Implement DashboardAuthMiddlewareLive using BetterAuthService to provide CurrentUser.
- Implement credential and installation scope middleware to provide CurrentCredential and CurrentInstallation from RPC payloads.
- Implement DashboardHandlersLive exactly matching the RPC contract.
- Map service errors to typed RPC errors from the contract.
- Ensure all credential-scoped handlers verify ownership before calling services.
- Do not implement dashboard behavior as REST except optional temporary debug mirrors if absolutely needed.

Dependencies:
- Depends on `TODO-23f38d31` for RPC contract.
- Depends on `TODO-a3b66ad4` for BetterAuthService.
- Depends on `TODO-1dac80e4` for context/scope helpers.
- Depends on `TODO-13e1922c` for credential/alarm/device services.
- Depends on `TODO-3293a61a` for shortcut/token RPC handlers.

Testing/validation:
- Use `RpcTest.makeClient(DashboardRpcs)` against handlers with stubbed services.
- Tests verify unauthenticated calls fail with typed Unauthorized.
- Tests verify credential ownership failures are typed and safe.
- Tests verify each RPC delegates to services and returns contract DTOs.

Notes:
- Keep handlers thin: decode/provide context/delegate/map errors.
