const root = document.querySelector("#root");

if (root) {
  root.innerHTML = `
    <h1>Verisure Dashboard</h1>
    <p>TanStack Start dashboard scaffold. RPC client and UI flows are implemented in later tasks.</p>
  `;
}
